# Streamlit GDF – Dashboards

A documentação está na pasta **documentacao_md**.

→ **documentacao_md/README_streamlit.md**

Resumo: um processo Streamlit, vários dashboards por token; registrar em `core/factory.py`; Django gera token com `tipo_relatorio`.
