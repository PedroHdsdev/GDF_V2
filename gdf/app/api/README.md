# API – Endpoints, Jobs e Tasks Celery

A documentação está na pasta **documentacao_md**.

→ **documentacao_md/README_api.md**

Resumo: jobs em thread (CargaXml, CargaSped), tasks Celery (carga agendada), funções `fn_api_*` em `app.views`.
